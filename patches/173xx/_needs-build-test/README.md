# 173xx patches — imported, NOT yet build-tested

These were pulled by import-patches.sh but do not apply cleanly to the current
NVIDIA source tree (strip level / context drift). They are held here, OUT of the
build, until the forward-port track (docs/forward-port-status/) fixes and
re-homes them into build/ or kernel/ with a PROVENANCE entry.

render-debian.py ignores this directory.
