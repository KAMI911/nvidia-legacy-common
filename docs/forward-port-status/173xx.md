# 173.14.39 → modern kernel — forward-port status

**Status: build-verified.** `nvidia.ko` compiles clean (zero warnings) on both
Ubuntu 20.04/kernel 5.4.0-200-generic and Ubuntu 24.04/kernel 6.8.0-139-generic,
via `patches/173xx/kernel/0001`-`0003`. Not yet runtime-tested on real GeForce FX
hardware, and not yet re-verified at the 6.12 target below (the two kernels
above were what was available to test against; nothing found so far suggests
6.9-6.12 need anything beyond what 0003 already does, but that's untested,
not assumed safe).

**Target:** compile-clean against Linux 6.12 LTS (amd64 + i386), runtime-validated
later on real GeForce FX hardware.
**Reference:** `patches/304xx/` — 304.137 shares the pre-modular layout
(`usr/src/nv/{nv.c,nv-linux.h,os-interface.c,nv.h,nv-vm.c,os-agp.c}`) and has a
maintained patch set to 7.0. Most 304 patches port to 173 with line-offset and
context fixups; 173 lacks `nv-drm.c`, `nv-mmap.c`, `nv-mlock.c`, `nv-procfs.c`,
`nv-pat.c`, so DRM/PAT/mmap patches are dropped and a few things live in `nv.c`
that 304 moved out.

## API breaks actually found and fixed (kernel/0003)

The table below was this doc's original *predicted* break list, written before
any of 173xx actually reached real compilation (that needed the separate
`kernel/0001` kbuild `M=` fix first). Once compilation was genuinely
unblocked, the real break list turned out to differ from the prediction in
specifics (found empirically, one `docker run` iteration at a time, against
real Ubuntu 20.04/5.4 and Ubuntu 24.04/6.8 headers) even though the general
shape — ACPI, timers, procfs, vm_flags, MTRR, tasklet/GPL symbols — rhymes
with the predictions below. The prediction table is kept for reference; treat
it as historical, not current. The actual fix list, with full detail
including *why* each conftest probe is written the way it is, lives in
`patches/173xx/PROVENANCE.toml`'s entry for `kernel/0003-modern-kernel-forward-port.patch`.

Notably, `spin_unlock_wait()` (flagged below as "the one genuine blocker,
hard, needs care") turned out to be no harder than the other API removals in
practice: this driver's one call site is a plain "wait for the lock to be
free" wait, not a partial-lock protocol, and the fix (`spin_lock(l);
spin_unlock(l);`) mirrors exactly what upstream Linux itself did at every
other in-tree caller when the function was removed. That said, this was
reasoned from reading the call site and from Linux's own removal precedent,
not from testing on real hardware under contention — see "Known gaps" below.

## API breaks 3.x → 6.12 and how each was *predicted* to be handled (historical)

| Kernel | Break | 173 file | 304 patch to adapt | Port difficulty |
|---|---|---|---|---|
| 4.3  | MTRR `mtrr_add` gone (config) | nv-linux.h | build/0001-disable-mtrr | trivial |
| 4.11 | `get_user_pages` sig | nv-linux.h | (in 0026 chain) | medium |
| 4.12 | 5-level paging `p4d` | nv-linux.h | kernel/0002-pud-offset | easy |
| 4.14 | `spin_unlock_wait()` **removed** | nv.c (RM lock) | **none — 173-specific** | **hard, needs care** |
| 4.15 | `init_timer` → `timer_setup` | nv.c | kernel/0004-timer | easy |
| 4.16 | `access_ok` drops `type` arg | nv.c, nv-linux.h | build/0005-usercopy | trivial |
| 5.0  | `do_gettimeofday` removed → `ktime_get_real_ts64` | os-interface.c, nv.h | kernel/0006-do_gettimeofday | **DONE — patch 0002** |
| 5.3  | `on_each_cpu` sig; kbuild `subdir-` | nv-linux.h, Makefile.kbuild | kernel/0007, 0008 | easy |
| 5.6  | `proc_ops`; `ioremap_nocache` gone; `MODULE_IMPORT_NS` | nv-linux.h, nv.c | kernel/0010-proc-ops | medium |
| 5.6  | `struct timeval` fully gone | nv.h, os-interface.c | (covered by 0006) | — |
| 5.7  | `set_memory_array_uc` unexported | nv-linux.h/os | kernel/0011 | medium |
| 5.8  | `pgprot`/`vm_flags`; `kernel_setsockopt` | nv-vm.c, nv.c | kernel/0012 (partial) | medium |
| 5.9  | `vmalloc` header split; `HAVE_UNLOCKED_IOCTL` | nv-linux.h | kernel/0017-vmalloc | easy |
| 5.11 | `NV_KMEM_CACHE_CREATE` slab flags | nv-linux.h | kernel/0018 | easy |
| 5.14 | `time64`/`__kernel_old_time_t` | os-interface.c | kernel/0019 | easy |
| 5.16 | `NV_GET_USER_PAGES` remote sig | nv-linux.h | kernel/0020 | medium |
| 5.17 | `PDE_DATA` → `pde_data` | nv.c | kernel/0021 (procfs) → adapt to nv.c | medium |
| 5.18 | `pci_free_consistent`→`dma_free_coherent`; `pin_atomic` | nv.c, nv-vm.c, nv-linux.h | kernel/0022 | medium |
| 6.0  | ACPI `acpi_bus_get_device` gone | nvacpi.c | kernel/0023-acpi | easy |
| 6.2  | `follow_pfn`; `acpi_dev_hid` | nvacpi.c, nv-linux.h | kernel/0024 | medium |
| 6.3  | `NV_VMA_FILE`; `class_create` 1-arg | nv-linux.h, nv.c | kernel/0025 (mmap→adapt) | medium |
| 6.4  | `class_create` single arg (final) | nv.c | (fold into 6.3) | easy |
| 6.5  | `get_user_pages` drops `vmas` | nv-linux.h | kernel/0026 | **DONE-able — patch template ready** |
| 6.6  | `MODULE_IMPORT_NS` quotes; `vm_flags_set` | nv-vm.c | kernel/(hangya 6.6) | easy |
| 6.8  | `strlcpy`→`strscpy`; `vm_fault_t` | nv.c | — | easy |
| 6.10 | `MODULE_IMPORT_NS` macro change | nv.c | hangya kernel-6.10 | easy |
| 6.12 | `follow_pte` sig; `MODULE_DESCRIPTION` required | nv-linux.h, nv.c | hangya kernel-6.12 / flydisco 6.15 | medium |

## Known gaps

- **No runtime testing.** Everything above is compile- and link-verified only
  (`nvidia.ko` produced, zero warnings, on 5.4 and 6.8). Nobody has loaded
  this module on real GeForce FX hardware. The `spin_unlock_wait()` removal in
  particular (see above) is reasoned to be safe, not proven safe under load.
- **ACPI display-switch handle detection (CRT/TV/LCD/DVI) is silently
  unpopulated** on kernels where `struct acpi_device` no longer exposes its
  own child list — the replacement enumeration API is `EXPORT_SYMBOL_GPL` and
  unusable from this proprietary module. ACPI event handling otherwise works;
  only this one piece of hybrid-display detection degrades.
- **6.9-6.12 untested.** Nothing found while fixing 5.4→6.8 suggests the
  remaining predicted 6.10/6.12 breaks (`MODULE_IMPORT_NS` macro change,
  `follow_pte` signature, `MODULE_DESCRIPTION` requirement) are still live —
  but that's genuinely untested, not verified absent.
- **i386 build untested** — only amd64 has been build-verified.
- **96xx/71xx** (siblings on the same installer generation) got the same
  `kernel/0001` kbuild `M=` fix opportunistically, but 96xx hits its own,
  separate, not-yet-investigated blocker (`conftest.sh select_makefile`:
  "Unable to determine the target kernel version") before it even reaches
  real compilation; 71xx has not been independently attempted at all.

## Progress

- [x] framework: feasibility-recheck.sh, forward-port.sh, _compile-one.sh (usr/src/nv layout)
- [x] patch 0001 — kbuild `M=$(PWD)` fix (real unblock)
- [x] patch 0002 — timespec64 (kernel ≥ 5.0) — build-verified
- [x] patch 0003 — full forward-port — build-verified clean on 5.4 and 6.8
- [x] first clean compile @ 5.4, then @ 6.8
- [ ] compile @ 6.9-6.12 (untested, not assumed clean)
- [ ] i386 build
- [ ] runtime on FX 5200/5500/5900 (hardware needed)
